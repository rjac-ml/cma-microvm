# Webhook signature verification (client.beta.webhooks.unwrap); the [webhooks]
# extra provides the HMAC verifier.
anthropic[webhooks]>=0.69

# Structured Logger and cached Secrets Manager retrieval.
aws-lambda-powertools>=3.0

# Vendored boto3/botocore (lambda-microvms client). Referenced by file path so
# pip installs exactly these wheels; botocore is listed first to satisfy boto3.
src/functions/wheels/botocore-1.43.34-py3-none-any.whl
src/functions/wheels/boto3-1.43.34-py3-none-any.whl
